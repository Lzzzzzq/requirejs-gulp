require(['inner', 'test'], function(inner, test) {
  var arr = ['foo', 'bar'];
  var arr = ['foo', 'bar'];
  var arr = [['foo'], 'bar'];
  var arr = [['foo'], 'bar'];
  var arr = [
    'foo',
    'bar',
  ];
  function ok(a, b, c){

  }
  if (true) {
  } else {
  }
  inner.init();
  test.init();
  console.log(123456789101234);
});
